#!/bin/sh
# bin/compile <build-dir> <cache-dir>

echo "-----> Removing cached .bundle/config"

if [ -f $2/.bundle/config ]; then 
  rm $2/.bundle/config
fi

echo "       Copying .heroku_bundle/config to .bundle/config"

mkdir -p $1/.bundle
cp $1/.heroku_bundle/config $1/.bundle/config
